import { StudyBlockFormatOption } from "../models/StudyBlockFormatOption";

export const FORMAT_1: StudyBlockFormatOption = {
    formatString:
`> [!gospel-study]
> # {{referenceLink}}
> {{paragraphs:
>
> }}

{{tag}}
`,
    name: "Default 1"
};